export default function BirthdaysPage() {
  const title = 'BirthdaysPage'.replace('Page','').replace(/([A-Z])/g,' $1').trim()
  return <div><h1 style={{ fontSize: 22, fontWeight: 700, marginBottom: 16 }}>{title}</h1><p style={{ color: '#6b7280' }}>Wird implementiert.</p></div>
}
